interface:
  display_name: "Reflect"
  short_description: "Session wrap-up with actionable follow-through"
  default_prompt: "Use $reflect to inspect this Codex session, summarize the changes, and identify which docs, learnings, and next actions actually matter."
policy:
  allow_implicit_invocation: false
