interface:
  display_name: "Autoresearch"
  short_description: "Eval-driven optimization for Codex skills and pipelines"
  default_prompt: "Use $autoresearch to define a repeatable run harness for this Codex skill or pipeline, establish a baseline with binary and comparative evals, and then run bounded mutation batches that keep only measurable improvements."
policy:
  allow_implicit_invocation: false
